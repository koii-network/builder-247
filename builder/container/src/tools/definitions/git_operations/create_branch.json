{
	"name": "create_branch",
	"description": "Create and checkout a new branch in the current repository",
	"input_schema": {
		"type": "object",
		"properties": {
			"branch_name": {
				"type": "string",
				"description": "Name of the branch to create"
			}
		}
	}
}
