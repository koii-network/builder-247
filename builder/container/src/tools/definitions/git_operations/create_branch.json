{
	"name": "create_branch",
	"description": "Create a new branch with automatic timestamp suffix",
	"input_schema": {
		"type": "object",
		"properties": {
			"branch_base": {
				"type": "string",
				"description": "Base name for the branch (without timestamp)"
			}
		}
	}
}
