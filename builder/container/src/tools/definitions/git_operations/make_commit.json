{
	"name": "make_commit",
	"description": "Stage changes and create a commit in the current repository",
	"input_schema": {
		"type": "object",
		"properties": {
			"message": {
				"type": "string",
				"description": "The commit message"
			},
			"add_all": {
				"type": "boolean",
				"description": "Whether to stage all changes",
				"default": true
			}
		}
	}
}
