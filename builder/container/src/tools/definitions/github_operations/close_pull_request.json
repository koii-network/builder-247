{
	"name": "close_pull_request",
	"description": "Close a pull request, optionally merging it",
	"input_schema": {
		"type": "object",
		"properties": {
			"repo_full_name": {
				"type": "string",
				"description": "Full repository name in format owner/repo"
			},
			"pr_number": {
				"type": "integer",
				"description": "Pull request number"
			},
			"merge": {
				"type": "boolean",
				"description": "Whether to merge the PR before closing",
				"default": false
			}
		},
		"required": ["repo_full_name", "pr_number"]
	}
}
