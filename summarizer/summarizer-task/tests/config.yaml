task_id: "summarizer"
base_port: 5000
max_rounds: 3

data_dir: data
workers_config: workers.json

mongodb:
  database: summarizer_test
  collections:
    todos:
      data_file: todos.json
      required_count: 1
    issues:
      data_file: issues.json
      required_count: 1
